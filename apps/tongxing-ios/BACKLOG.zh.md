# 同行 iOS 产品 Backlog

更新：2026-09-12。IOS-01 至 IOS-04 已有首版原生代码，但各项不等于完成全部验收。下面新增 Duo 工具到位后的开发任务与灵动岛后续任务；Apple 规范、产品选择和当前源码缺口见 [平台设计提炼](APPLE-PLATFORM-DESIGN.zh.md)。当前版本与证据见 [README](README.zh.md) 和 [Beta 验收](BETA-TESTING.zh.md)。仓库的[旧 backlog](../../docs/backlog.zh.md)仍是历史 Web/实时字幕快照。

| 编号 | 需求 | 拆分与依赖 |
| --- | --- | --- |
| IOS-01 | 使用期间的灵动岛体验 | 已有播放状态扩展；系统媒体优先方案、专属状态与实际展示仍待验收 |
| IOS-02 | 点击按钮后短时启用麦克风，后台识别现场进度，自动定位并关闭麦克风 | 仅同一录音重放；先验证指纹与共用时间轴的中文同步音轨 |
| IOS-03 | 多语言，首先支持英文 | 先完成中英文界面；内容语言、音轨语言与界面语言分别管理 |
| IOS-04 | 字幕全文增加英文原文 | 中文在上，英文在下；英文更小、使用不同的次级文字颜色 |

既有实现：ActivityKit + WidgetKit 扩展、中英界面及偏好保存、来源块关联双语全文，以及 8 秒本地麦克风采集和 Swift 指纹定位。此前 2026-09-07 记录了 28 项模拟器与 28 项真机测试、4 项模拟器 UI 测试通过，属于对应版本的历史证据。本次自适应布局的 8 项实际 UI 测试与限制见 [适配记录](DUO-ADAPTATION.zh.md)，不计为灵动岛展示验收。发布状态与剩余验收见 [发布清单](RELEASE-READINESS.zh.md)。

## IOS-01：灵动岛与锁屏

目标：收听或短时对齐时，离开 App 仍能看见当前状态并快速回到操作。

- 播放场景先检查现有 `MPNowPlayingInfoCenter` / `MPRemoteCommandCenter` 的系统媒体体验，补齐标识图片、标题、进度及播放状态同步。系统媒体区域的布局由系统决定。
- 当前 ActivityKit 扩展在普通播放时已经启用，但只展示播放状态。后续先比较它与系统媒体展示的重复程度，再按需加入“采音中 / 匹配中 / 本次定位完成”等专属状态；这不是现有能力。切后台即取消采音的限制解除并验收前，不以灵动岛宣称可后台对齐。全文与大纲留在 App 内。
- 所有操作仍交给唯一的 `PlaybackController`；Live Activity 只是状态与操作入口，不另建播放器，也不充当后台运行保证。
- 暂停、换篇、播放结束、对齐取消/超时后更新或结束对应状态；不支持灵动岛或用户关闭实时活动时，保留正常 App 和锁屏媒体操作。

验收：实体 iPhone 检查紧凑、展开、多个活动并存、锁屏及横屏展示；播放/暂停/定位与 App 一致，无重复或过期活动。具体可用布局按实际系统验证。自定义活动须覆盖所需展示形态，依据 [Apple Live Activities 文档](https://developer.apple.com/documentation/ActivityKit/displaying-live-data-with-live-activities)。

## IOS-02：同一录音重放，一键指纹对齐

**当前范围：只考虑同一份英文录音重放，正常1倍速。** 本版采用与已发布 Web 内容共用的版本化指纹索引和纯 Swift 本机匹配器，暂不开发 ASR、关键词、重新讲述或视频画面匹配。完整分析、现有数据证据、时间补偿和实验步骤见 [音频指纹对齐方案](FINGERPRINT-ALIGNMENT.zh.md)。

用户点击“听现场并对齐” → 短时采集英文 → 本机匹配录音位置 → 校验目标音轨及时间原点 → 自动定位中文 → 关闭麦克风并恢复播放意图。支持取消、超时、失败回退和撤销；切后台会取消本次采音并恢复此前播放意图；后台继续匹配尚未实现，不持续监听。

首版优先使用按英文时间轴布置的中文同步音轨。已有同步候选制作链路可复用；普通自然语速音轨不能直接用英文秒数定位。指纹可以先独立实验，不以完成双语全文或现场 ASR 为前置条件。

主要交付：

- 为每篇英文参考录音生成指纹目录，并发布绑定来源、时间原点、目录哈希和中文音轨哈希的版本化能力清单；下载后可离线使用。
- 验证当前同步候选的时间原点及放置质量，保留审核状态；不因声学指纹成功而宣称英中语义同步已通过。
- 本机短时采音与匹配，记录采样时钟并补偿处理延迟；iOS 17 不依赖 iOS 18.4+ 才提供的 `confidence`。低置信或位置不唯一时保持原进度。
- 所有退出路径关闭采音；换篇、换轨或手动操作使旧请求失效，迟到结果不覆盖用户操作。日志不默认保存现场录音。
- 分别验证文件匹配、扬声器到手机的声学匹配、中文实际播放位置以及后台执行；记录成功率、拒绝率、误跳率、位置误差与总耗时，不把模拟器或文件成绩当成现场证明。

英文原文与英中块关系仍由 IOS-04 补齐供双语阅读使用；自然音轨的更细跨语言映射留到后续需求。

## IOS-03：多语言，英文优先

目标：首批支持简体中文与英文界面，按系统/App 语言选择，保留明确回退；采用 String Catalog 管理按钮、错误、下载状态、权限用途说明、无障碍标签和系统媒体文案。

界面语言、字幕语言、音轨语言分开管理。切换到英文界面不隐式更换正在播放的音轨，也不将中文内容反译后称为英文原文。先接入已有英文原文；英文原声播放作为该条目的后续子项，须有可发布/缓存的来源音轨、时码与完整性信息后才能开放，不能把语言切换当成已具备英文音频。

验收：完整核心收听/下载/恢复/精调流程无遗漏硬编码文案；中英切换和重启后符合所选语言，进度与缓存保持；英文长文本、日期、数字、较大字号、VoiceOver 及后续灵动岛均有检查。未提供某语言的内容明确显示实际语言和回退情况。

## IOS-04：字幕全文中英双语

目标：全文每个对应内容单元先显示中文，再显示英文原文。英文使用更小的动态字号和可适应深浅色的次级文字颜色；与中文形成层级，同时保持可读性，不能只靠颜色辨识语言。

- 通过来源段落/单元关联显示，保留一对多、多对一和缺失状态；不能按两个文本列表的行号拼接，也不能把中文回译成“英文原文”。
- 英文缺失时标记“英文原文暂缺”，保留中文和播放；整篇缺失时避免重复堆叠提示。英文来源和审核状态保留，不因展示为双语升级为人工审核。
- 当前中文音轨仍决定全文高亮与时间按钮；点击时间只触发现有定位。英文源视频时间如需展示，须与当前中文播放时间明确区分。
- 完整英中内容关系随离线资料缓存；缺少新增字段的旧目录保持可读，来源/音轨切换后不串段。

验收：抽查首段、中段、末段、重复经文、一对多及缺失案例；中文在上、英文在下且更小、颜色不同；深浅色、增强对比、大字、窄屏与横屏不截断；VoiceOver 按中文后英文顺序并使用相应语言朗读。全文滚动、回到当前句、时间定位及离线读取保持有效。


## IOS-05：Duo 自适应布局与完整工具验收

当前单栏/双栏首版已实现并通过通用模拟器测试。默认左栏大纲、可切全文，右栏当前字幕；正式 Duo 任务仍开放。以下 P0 表示 Duo 支持声明前的必需工作，P1 表示后续体验完善，不要求阻塞已有普通 iPhone 功能。

| 编号 / 优先级 | 当前状态与依赖 | 后续交付 | 关闭条件 |
| --- | --- | --- | --- |
| IOS-05A / P0 | 等待官方 Xcode 27.1 beta 和 Duo runtime；核对日 2026-09-12 尚未提供 | 核验下载来源、build/SDK/runtime；进程级工具链；建立 Device Hub 运行方法 | 同一源码可构建和启动，记录环境及可复用命令；不以 27.0/iPad 替代 |
| IOS-05B / P0 | 等待 05A | 校准列宽与阈值；检查内外屏、Split View、侧边导航、Sheet 与 overflow | 当前字幕可读，阅读/精调/播放可达；同一音轨、播放意图和两种阅读锚点跨开合保留 |
| IOS-05C / P0 | 等待 05A、05B；核对 SDK 签名后实现 | 相机遮挡、折叠分区、各边 safe area；自定义 PlaybackDock 避让 | 平展、书本半折、桌面折叠、相机激活时按钮与字幕不被挡；全文不无故跳位 |
| IOS-05D / P1 | 等待 05C；独立实验 | 比较 NavigationSplitView 与 ArrangementView，验证上下/左右内容布局 | 保持层级和唯一播放器；有可读性与触达改善证据才迁移，否则保留现状并记录结论 |
| IOS-05E / P0 | 依赖 05B、05C；需 Duo 模拟器及实体设备 | 完成显示与状态矩阵，包含键盘、多任务、长英文、最大字号、深浅色、增强对比和减少透明/动态效果 | 模拟器与真机结果分开记录；播放/耳机/中断/锁屏实测通过，无重复播放器或错误 seek |

矩阵最低覆盖：外屏 → 内屏 → 半折 → 外屏，播放和暂停各一轮；阅读大纲/全文各一轮；内屏宽/窄 Split View；Sheet 打开时转换；Duo 灵动岛/系统状态占位出现时再检查侧边操作。05D 不能成为未经体验证据的架构重写。

## IOS-01 后续：灵动岛产品与工程验收

这些任务细化 IOS-01，不将现有扩展误写成尚未实现。除 01F 的 Duo 专项外，其余设计、普通 iPhone 开发与验证可使用现有工具推进。

| 编号 / 优先级 | 当前状态与依赖 | 后续交付 | 关闭条件 |
| --- | --- | --- | --- |
| IOS-01A / P0 | 待做；实体灵动岛 iPhone | 补系统媒体品牌图片；比较 Now Playing 单独展示和同时开启现有活动；决定自定义活动默认与开关 | 普通收听信息不重复干扰，控制一致；形成保留/按需启用的明确结论 |
| IOS-01B / P1 | 待设计；对齐活动启用依赖 IOS-02 真实后台能力验收 | 定义采音、匹配、映射校验、定位成功、失败、取消和过期状态；绑定会话与来源 | 采音停止即不再显示采音；定位成功不声称持续同步；无后台能力时只显示 App 内状态 |
| IOS-01C / P0 | 待做；先基于当前活动完善 | 定义源身份深链、冷/热启动恢复、用户关闭、长暂停、结束/失败清理和 stale 策略 | 旧活动不切换新来源或 seek；用户关闭不反复重建；移除活动不停止音频；过期不继续显示虚假实时进度 |
| IOS-01D / P1 | 待做；依赖 01A、01B、01C | 按需验证一个 App Intent 动作，如取消；撤销定位先在 App 内验证事务条件 | 锁定/解锁与快速重复点击结果正确；唯一播放器执行；来源变更后的迟到动作无效 |
| IOS-01E / P0 | 待做；可先做预览，再上真机 | compact/minimal/expanded/锁屏矩阵，长中英标题、VoiceOver、低亮 Always-On、StandBy、多个活动竞争；标题隐藏选项 | 最小形态可识别；不靠颜色/动画区分状态；隐私选择与实际呈现一致，无正文截断成误导状态 |
| IOS-01F / P0（Duo） | 等待 IOS-05A；依赖 01E | Duo 侧边活动与工具栏并存，以及开合后的展示和深链回到正确区域 | 系统调度/无岛或关闭活动时正常降级；播放、精调和内容均可达，记录 Duo 真机证据 |
| IOS-01G / P1 | 待做；依赖 01C | 单独测试 coordinator 生命周期和 payload 大小、更新合并、挂起/中断/恢复；为真实 ActivityKit 验收准备不禁用活动的构建入口 | 非每秒业务推送，事件及时反映；4 KB 内；过期策略与实际显示一致；不把禁用活动的 UI 测试当验收 |

顺序：01A/01C/01E → 01G；只有对齐后台能力与增量价值成立，再推进 01B/01D；05A 到位后推进 Duo 任务和 01F。本清单仅记录后续工作，不创建自动下载或后台定时任务。


## IOS-06：跟进 2026-09-13 main 的网页能力

本轮已同步主线原生代码，Cloud Build 16 的源码为 `41efc38`；具体回执见 [TestFlight 同步记录](release/testflight-build16-2026-09-13.zh.md)。下列项目是原生端仍未接入的主线能力，不影响当前普通收听和阅读分发，也不计为已开发完成。

| 编号 | 当前缺口 | 后续交付与关闭条件 |
| --- | --- | --- |
| IOS-06A | 网页读取 `week.audioFingerprint`；原生只支持 `track.alignment`，发行器当前拒绝发布后者 | 先冻结主线 C3 能力清单和样本，核对 schema、算法、源视频时间原点、索引与音轨哈希；建立原生兼容层及 malformed/过期/跨来源拒绝测试。可靠文件匹配、真实扬声器到手机匹配及中文定位分别验收；不放宽发行器门槛来假装兼容。 |
| IOS-06B | 网页可靠匹配后自动播放；原生暂停发起匹配成功仅定位 | 依赖 06A。明确“对齐并播放”动作契约后接入：可靠命中才定位并开始，失败/取消/手动暂停/切源后的迟到结果不得误播；普通手动定位继续保留暂停意图。需 UI、事务和真实音轨验证。 |
| IOS-06C | 网页 PDF/原视频时间轴资料下载和反馈统计未移植 | 分别评估原生资料入口与自愿反馈需求，复用已发布白名单和数据契约；反馈须重新核对原生隐私披露、开关和删除语义。网页默认或授权不自动迁移到原生沙盒。 |
