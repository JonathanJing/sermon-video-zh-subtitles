{
  "name": "local-live-poc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run test:unit && npm run test:integration && npm run build",
    "test:unit": "node --test tests/frontend/*.test.mjs && python3 -m unittest backend.tests.test_content_pack backend.tests.test_session_store -v",
    "test:integration": "python3 -m unittest backend.tests.test_gateway -v",
    "test:backend": "python3 -m unittest discover -s backend/tests -v"
  },
  "dependencies": {
    "@vitejs/plugin-react": "5.0.4",
    "vite": "6.4.2",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {}
}
