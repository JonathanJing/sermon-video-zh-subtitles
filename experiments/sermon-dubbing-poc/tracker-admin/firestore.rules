rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /sermonTrackerRuns/{pageId} {
      allow get, list: if true;
      allow create, update, delete: if false;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
