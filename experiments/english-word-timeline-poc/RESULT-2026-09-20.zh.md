# 英文逐字稿与词级时间轴 POC：2026-09-20 首轮记录

## 结论

独立的 transcript-first 链路已能在 MacBook 本地完成“英文 ASR 候选 → ForcedAligner 逐词时间 → 缺口报告”，但首轮真实片段**没有通过结构门槛**。固定版本的 Qwen ForcedAligner 在 191 个候选词中给 3 个短词返回零时长；这些词没有被插值或静默丢弃，报告状态为 `invalid_alignment`。因此当前模型组合只能继续作为 POC 候选，不能接入正式翻译或配音放行。

## 输入与边界

- 使用仓库已有的 2026-09-13 本地生产音频，source ID `8JYwTq1xcBE`；没有重新下载媒体或调用云端转写。
- 取 sermon clip 内 `300–360` 秒，共 60 秒；已知完整礼拜偏移为 1,606 秒，所以输出同时保存 clip 内时间和完整礼拜 `1906–1966` 秒时间。
- 当前问题周次 2026-09-20 的批准原声不在这个 checkout；本轮验证代码与模型行为，不是对当日漏译段落的复现或修复证明。
- 英文全文和逐词表只保存在忽略目录 `artifacts/english-word-timeline-poc/`，不提交 Git。

## 真实 60 秒结果

运行目录：`artifacts/english-word-timeline-poc/20260920-real-60s-v2/`

| 项目 | 观察值 |
|---|---:|
| ASR / 对齐窗口 | 1 个，60 秒 |
| ForcedAligner 候选词 | 191 |
| 结构有效、写入逐词表 | 188 |
| 零时长词 | 3 |
| 其他时间轴重叠 | 0 |
| 最终状态 | `invalid_alignment` |
| `transcriptCompletenessProven` | `false` |

三处失败均为 ForcedAligner 的 `start == end`，涉及短词或弱读词。POC 将其记录为 `invalid_aligned_word / zero_or_negative_duration`；没有根据相邻词边界补造时间。报告、TSV 和英文候选的 SHA-256 分别为：

- `report.json`：`e55bf747bba70cd8b8d3f611bad3bca164e0b32e1cd904f2d7dfd20e30add6fe`
- `word-timeline.tsv`：`9ab16f619e3b15eeb6fccbb5f12450cf9122f819d06d2a4c867f1465884504df`
- `transcript.txt`：`d2354c757c1f938de3cfdd058c1dd380fe29a9a055d6b71223df61b06b367de1`

这些哈希绑定的是修正窗口规划后的 v2 产物；随后代码只增加失败原因字段，未回写或冒充重跑该产物。

将窗口缩短到 30 秒、重叠 5 秒的 A/B 结果更差：3 个窗口合并后只有 186 个结构有效词，出现 1 个 ASR／aligner token 序列差异及 5 个零时长词。因此当前不采用“缩短窗口即可解决”的假设。

## 静音负例

5 秒精确零 PCM 在模型加载前被拒绝：ASR 和 ForcedAligner 都没有调用，`wordCount=0`，状态为 `no_word_timeline_requires_review`。这证明静音不会被本入口自动升级为可用英文时间轴；它不证明非零噪声、音乐或掌声不会产生 ASR 幻觉。

## 下一步

1. 已用同一段音频和同一份冻结英文候选完成 MFA 对比；MFA 在该样本通过结构门槛，详情见[对齐器对比记录](ALIGNER-COMPARISON-2026-09-20.zh.md)。两种输出仍都保持机器候选。
2. 增加人工听音界面或审核表，逐词抽查专名、数字、否定、重复、窗口边界及报告中的所有缺口。
3. 取得 2026-09-20 的批准原声后，只跑用户指出的异常段落，比较原生产英文、重新 ASR 和两个 aligner，不先重做中文或音频。
4. 只有英文逐字覆盖和词级时间轴通过后，才设计英中逐句覆盖报告与自然语速门槛。

本轮只确认 POC 入口和失败保护真实工作；尚未选出可进入生产的最终模型，也未完成人工逐字稿或当前周次验收。
