# 2026-09-05 Astra 口播修订与同步候选

8 月 30 日《当我愤怒时》已生成并发布 29 分 30 秒同步试播候选。文字修订与审核均在同一 Codex 对话使用 GPT-6 Astra；这份报告记录本次产物，不表示每周全自动或现场同步已验收。

## 实际结果

- 对照完整英文审阅 55 段，修订 18 段中文；其中两段再次缩短后重新合成。原 PDF、阅读稿、英文父版本均保留。
- 原声风险片段转写确认第 32 段应为 `to not shield`，修正否定关系及误转写的“今晚”；引文边界独立修订。
- 最终 118 个合成单元。第一轮复用 85 个 WAV、生成 34 个；第二轮复用 115 个、生成 3 个。缓存的 WAV、生成回执及逐段 ASR 按内容和哈希绑定复用。
- 9 处弱声学边界经 Astra 证据审核，全部保留原起止。15 段超时经文字重审、再合成和一处明确播放编排消除；55 段最终时槽检查无失败。
- 最后一段“现在一起敬拜”完整时长 1.92 秒；英文锚点仍为 1768.88，中文播放起点为 1768.08，明确提前 0.80 秒。它晚于前段英文结束 1768.00；前段中文结束 1762.58，两段中文间隔 5.50 秒。该原声间隔是低能量区间，不能声称绝对静音或口型同步通过。
- 同步 WAV 长度 42,480,000 样本 / 24 kHz。逐块对比原自然语音，波形误差为零，无截句、叠音或变速。MP3 完整解码通过。

## 模型审核的剩余听感问题

118 段 ASR 全覆盖，61 条差异全部经过 Astra 逐项复核：56 条同音/书写差异、3 处专名声调疑点（奥波格、雅各）、2 处 ASR 或轻微口语插字。三处声调疑点与两处口语差异保留在试听清单；不能将模型文字比对说成人耳听感验收。音色相似度、完整同视频试听、实体手机/蓝牙和真实场地仍待各自实测。

## 发布与验证

[试听应用](https://ai-for-god-sermon-audio.web.app)默认显示 8 月 30 日同步候选，并保留 8 月 23 日三份 Eric 对照样片及另外五位讲员音色试听。同步音轨的 00:00 对应原视频 29:00（批准片段起点），不是整部直播视频的零点。

新发布目录：`artifacts/sermon-dubbing/2026-09-05-weekly-app-v5-astra-sync`。21 个线上文件完整哈希匹配本地产物；音频 Range 206 与首 1,024 字节匹配。浏览器已验证 29:30 时长、跳转至 29:28.10 的末句字幕、0.25 秒微调、播放响应、大纲弹窗和历史周次保留。

102 项 Python 测试与 8 项前端测试通过。测试覆盖模型审核身份、父版本与输入冻结、WAV/ASR 缓存失效、边界证据、独立播放编排、同步候选发布及两路来源桥接。13 个原有无关修改文件逐字节保持不变。

## 两路来源与定时接线状态

主路等待周日实际播放的同版本纯证道视频及接入器；未来明确纯证道契约成立后可使用整片范围。直播归档 fallback 已能通过桥接器复用人工窗口、双 PDF QA 和六位讲员登记的检查点，来源分开加锁；自动边界证据契约尚未实现，当前仍属半自动。

真实 8 月 30 日 v4 桥接检查通过；9 月 6 日两路均等待真实来源。应用定时更新接口本轮未返回，回读仍是原双 PDF / Context Pack 任务，因此定时配音接线尚未确认。完整更新请求与回读结果保存在本机审核目录的 `automation-update.json`、`automation-status.json`；后续只需恢复此接线，不重做已经通过的音频与审核。

## 冻结证据

| 产物 | SHA-256 |
|---|---|
| v4 job | `93e0e8265b16d9ae88dcd8ae005581a6851839b71a85283cf20915961b5148ad` |
| 同步 MP3 | `d1a8d935aa35ecb7950caeddac6ff411119ac3777f1f2a2b869a4a7b0ffc64d3` |
| 时槽报告 | `ce29ee898a256bdd74570d4059e2d758ff46657aeb1eed06828e40f665f17497` |

本机音频：`artifacts/sermon-dubbing/2026-09-05-weekly-20260830-v4-astra`；Astra 审核：`artifacts/sermon-dubbing/2026-09-05-astra-editorial-review`。模型审核字段始终为 `humanApproval=false`。历史周六生成失败/未完成状态保留，候选发布不绕过正式周日完成门槛。
