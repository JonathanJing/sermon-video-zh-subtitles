# 未提交功能集成记录 · 2026-09-13

本次从 `592de8d` 建立独立集成分支，冻结主工作目录已有改动，避免覆盖并行任务。声音指纹 C3 与命中后自动播放此前已通过 PR #20、#21 进入 main；此次保留其实现。

## 功能范围

| 范围 | 纳入能力 |
| --- | --- |
| 同行网页 | 收听进度与微调恢复、定位撤销、播放加载与重试、正式版显示、双语全文、PDF/原视频时间轴资料下载、品牌与入口整理 |
| 反馈与统计 | 自愿反馈、沿用默认开启且可关闭的统计与撤回、离线队列与幂等重试、受限服务端 API、汇总报告和部署脚本 |
| 每周生产与发行 | 同源视频/历史证道准备、审批与源绑定、断点续跑、不可变发行 registry、下载文件白名单、HTTP 与 Range 核验 |
| 生产保护 | 租约续期与 fencing、父进程失效保护、下载完整性和移交、源文字审查路由、Saturday harness、追踪导出与可选 Temporal 控制层 |
| 本地字幕与实验 | 运行时重启/重连、启动进程监督，以及保持实验边界的移动音频探针与固定样本 |
| 原生 iOS | 未提交的全文打开定位，以及已有 PR #18 的布局与发布准备变更；保留 main 的旧 SDK 蓝牙兼容处理 |

## 合并修复

- 保留 main 的严格指纹字段、有限数值、媒体绑定与大小校验，叠加下载与反馈发行支持。
- 保留原 standalone 指纹发布测试；registry 测试单列为 `test_fingerprint_release_registry.py`，避免覆盖已有回归覆盖。
- 同源视频测试夹具补齐系列术语证据，生产验证不放宽。
- 保留最新自动播放说明；纠正文档中已过期的调度和 Cloud Run Job 表述。

## 验证与边界

候选代码已通过 root Python 1,100 项、dubbing Python 296 项、网页与反馈 Node 193 项。Promptfoo 本地工具和媒体检查执行 3 项通过；1 项真实基线回归因独立工作区没有原始媒体/审查收据而跳过。原媒体、凭据、下载文件、生产 registry 与运行日志不纳入 Git。

本次检查与合并不执行网页部署、付费生成、调度切换或 App Store 提交。文档中的历史发行 ID、日期、测试数量与设备观察只描述对应运行；不能视为本次重新完成了现场收音、实际音轨听审或生产服务验收。最终 PR 的 CI 与原生/本地字幕定向结果记录在 PR 描述及交付收据。

冻结快照之后其他任务继续修改的内容保留在原工作目录，按各自完成状态提交。旧设计分支和历史 PDF 分支需要先按当前架构去重，不整体覆盖 main。
