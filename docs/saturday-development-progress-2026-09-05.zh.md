# 周六流程开发进度核验（2026-09-05）

本轮检查发现，除视频、手机与现场实测外，仍有软件缺口：配音定时入口未接通，同版纯证道视频接入器未实现，网页仍指向较早音轨，反馈发布工具不能保留旧页面的来源白名单，以及新增日志字段尚待验证。以下逐项记录补齐结果；历史报告不改写为当前服务状态。

## 已有流程与本轮记账验证

- 8 月 30 日直播 fallback 的既有来源、人工窗口、76 个英文来源段、55 个阅读块和双 PDF 已完成历史验证；本轮重新核对绑定产物。阅读 PDF 37 页、解读 PDF 5 页。详见[完整验证报告](saturday-full-validation-2026-09-05.zh.md)。
- 修订配音为 119 单元、55 块，同步候选 29:30；时间预算与波形完整性证据通过。ASR／模型审核没有改写成人工听审。
- 新记账 v2 已保存子流程身份／状态、实际已加载代码 hash、业务证据前后快照、工作量、逐次 API／退避、延迟分位数、用量覆盖、本地 RSS／CPU 和磁盘余量。新旧 schema 共存，缺失历史值不补造。详见[日志字段和计量边界](workflow-accounting.zh.md)。
- 在完整缓存上实际运行配音入口：`runId=466ca1f7481746eeb2f3dad2c130ce11`，完成用时 1.736378 秒，API 尝试 0，119 个单元收据全部保留。本次新生成 0；历史修订继承 90 单元、重做 29 单元。历史 TTS 198.977 秒另列为模型加载后的旧报告计时，没有算成本次渲染。
- 该次新日志收集 11 项配音业务证据，缺失类别 0、读取错误 0；代码身份记录 9 个已加载项目模块。周次生产目录另验证 14 项业务证据，保留历史 Context Pack 已过期和同篇身份未确认状态。
- 中断留下半行 JSONL 时，已修复为保存原始字节、汇总有效事件并标记不可归属的账目缺口；再次运行可以继续追加。损坏记录不会静默当作免费或成功。

主要证据位于 `artifacts/saturday-validation/2026-09-05-aug30-astra/`：`development-audit.json` 是阶段审计，`development-cached-smoke-final.log` 和配音 `accounting/` 是本轮实际续跑记录。阶段检查结果与发布收据另行保留，不把测试成功当作模型重新执行。

## 日志补齐（同日后续）

用户随后要求建立 logging。本地生产入口和 Supervisor 已接入同一套运行／阶段关联，新增固定 code 的分级日志、项目内脱敏错误位置，以及只读 `scripts/sermon_logs.py` 查询／检查入口。独立线程不再串账；写盘失败不覆盖原业务异常；元数据和请求设置集中白名单过滤；合法 JSON 的结构损坏也会报告。新文件默认 0600、新账本目录 0700；原始账本保留，JSON／CSV／可读日志可重建。

Supervisor SDK 聚合请求数与 Token 单列在 `sdkCalls`，不冒充直接 HTTP 收据；内部重试、费用和缺失用量明确标未知。使用方式和范围见[统一日志说明](workflow-accounting.zh.md#统一事件与错误追踪)。

本次最终回归：根目录完整 unittest 840 项、配音目录完整 unittest 149 项全部通过；定向日志故障／入口／查询测试包含在其中，不重复加总。日志分别为同证据目录的 `logging-root-tests.log`、`logging-dubbing-tests.log`。

真实缓存续跑 `runId=544b410f564441e4b77094355687ff5b`，墙钟 1.715908 秒；123 个 WAV／MP3 文件 hash 全部保持一致，API 尝试 0、SDK 调用 0。新只读日志 CLI 的 `--check` 返回 0，无错误、未结束或损坏记录。证据为 `logging-cached-smoke.json`、`logging-validation.json` 与该配音任务 `accounting/operations.log`。这是日志和缓存路径验证，不是重新生成、人工审听或现场验证。

## 发布与同版来源接入

同版来源适配器已实现。`source`／`archive`／`reviewed-handoff` 使用独立版本化契约，job 保持 renderer 的 v1 单元结构。软件测试覆盖首次准备、分块 ASR、同源校验、无旧 pipeline PDF 的独立封存、共享候选验证、缓存续跑、合法视频 ID、主路损坏后的 fallback，以及封存重试不重复付费生成。默认 inspect 保持只读；初始化、缓存核验和封存写入自己的账本。

另外真实执行了两类本地检查：2 秒 FFmpeg 合成视频经原生 CLI 初始化、再次初始化与 inspect，确认原片字节不变、未写人工窗口批准、缓存核验 0.162595 秒且无 API 调用；隔离临时目录中的两份 PDF renderer 运行退出 0、双 QA 通过。后者未保留的实际哈希明确为未知，不把临时演示等同于真实来源封存。证据分别为 `development-same-video-cli-smoke.json` 和 `development-same-video-pdf-smoke.json`。

网站兼容发布工具已完成，位于 `experiments/sermon-dubbing-poc/deploy_feedback_compat.py`。它必须指定已验证的 API 快照，核对完整模块依赖与文件 hash，再合并明确列出的新旧发布来源，保留旧页面反馈和撤回能力。它不从工作区读取正在变动的 handler。

本轮新音轨已装配到 `artifacts/sermon-dubbing/2026-09-05-weekly-app-v10-saturday-validation`。与其构建时的 v9.1 相比，只变化当前周内容和同步 MP3，音色库、历史周及 UI 相同。发布前发现其他会话已更新同站点，随后其 GUI 收据又记录了待修复的并发问题；因此本轮没有覆盖线上版本。候选包需在那边最终验证版本稳定后重新合并和发布，不能把当前 v10 包认定为已上线或最终可发布版本。

| 验证 | 结果 | 收据 |
|---|---|---|
| 主流程、记账与业务证据 | 120 项通过，另有 5 个子测试 | `development-root-final-tests.log` |
| 配音、同版接入、旧 bridge、候选与恢复 | 110 项通过 | `development-dubbing-tests.log` |
| 新旧来源兼容发布 | 17 项通过 | `development-feedback-compat-tests.log` |
| 当前历史来源 bridge 只读回检 | 选择 `live_archive`、等待当前对话审核；同版路等待源；未写人工批准 | `development-bridge-final.json` |

这些测试不调用付费模型，也不证明真实视频、手机或现场验收。

## 仍不能认定完成的验收

- 原生周六任务 `pdf-context-pack` 当前为 `ACTIVE`，但读取到的 prompt 未调用配音桥接。2026-09-05T18:44:06Z 检查时，本会话没有原生任务更新工具；此前 Codex App 的 Computer Use 访问被安全限制拒绝。因此未手改配置文件或另建调度器，也不宣称已全自动。
- 同版纯证道视频尚未提供。软件合成输入测试无法证明真实来源和周日实际播放的是同一版。
- 最新候选仍有两个需实际听辨的问题：单元 5 的姓名“波／伯”，单元 112 的“雪／血”。它们是 ASR 差异筛查疑问，尚未证实为音频错误。
- 整篇音色、中文自然度、与视频播放同步，以及手机后台／蓝牙和真实现场体验仍待实测；不在本轮软件测试范围内。
- 旧的 114 条翻译／阅读解析缓存缺 Token 和逐请求时长收据；已知费用小计 $0.53283 只覆盖解读响应。GPU 峰值、远程模型加载、进程外审核和非 API 成本尚无完整计量。

本轮未执行 Git 提交、push 或合并。工作区其他并发修改保持原样；公开试听候选也不代表周日正式验收通过。
