# 2026-09-20 第二片段四层 POC：Firebase Dev 已发布

记录时间：2026-09-24。分支：`codex/release-2026-09-20-second-clip`。页面 ID：`2026-09-20-laodicea-clip`。本报告只记录本轮已观察的结果，不能替代产物收据、公开 HTTP、设备或现场验收。

## 片段与人工决定

- 原录像 38:07.32–40:25.32，片段 138.004534 秒；用户已批准这一范围。
- 33 个英文 source units 已有人审；简中、韩语、西语的 Layer 2 全文候选已获用户批准。Eric 声音用于本片段 Dev POC 的授权已由用户明确给出。
- 中文音频首轮听审中，用户指出第 01 组要在“老底嘉教会”后停顿、再读《启示录》，第 02 组经文应读作“三章十六节”，并表示其他内容通过。两处修订后，用户在修正版听审页答复“通过，继续生成”；新音轨的全文 1× 听审、视频同步与第 02/08 组机器疑点人工裁决已绑定新音轨哈希形成独立收据。
- 韩语新候选的 11 组全文已获用户批准。其候选绑定的正式音轨经完整 1× 播放与视频同步听审，用户答复“全文与同步都批准”；11 组、各项听审和音轨哈希已写入独立的人审收据。
- 西语正式音轨的全文与视频同步已获用户批准；三语页面信息稿也获得“三语页面信息全部批准”的独立决定。收据按音轨及页面提案 SHA-256 记录，不沿用文字审核决定。
- 片段、锚点、四层状态和操作收据位于忽略的 `artifacts/multilingual-clip-20260920/20260920-blocks15-18-138s/` 与 `artifacts/tracker-runs/2026-09-20-laodicea-clip/`。这些媒体与收据不会入 Git。

## 当前结果

| 项目 | 已观察结果 | 尚需完成 |
| --- | --- | --- |
| Layer 1 | 4/4；来源、英文、锚点、人审已记录 | 无 |
| Layer 2 | 三语均 4/4；韩语时长修订版 11 组重新人审并形成新收据，旧候选的下游引用已失效 | 无 |
| Layer 3 简中 | 第 01/02 组定向重合成，另 9 组 WAV 哈希未变；新整轨 138.004542 秒、排程通过；第 02 组 ASR 为“第三章第十六节”。机器第 02/08 组疑点已由用户听审结论裁决，Audio Package 为 `human_reviewed` | 实体 App／现场验收 |
| Layer 3 西语 | 6/6；11 组、138 秒 WAV、排程、字幕、完整回转写筛查通过；全文 1× 与视频同步听审批准，Audio Package 为 `human_reviewed` | 实体 App／现场验收 |
| Layer 3 韩语 | 6/6。正式合成复用同声音且指令未变的前 6 组 WAV；第 07/08/11 组采用已实测的语速指令。新整轨 SHA-256 `92f77059ce4f71be112dd6cb44beef7be98077c59d6041a6f8616cf94ce44710`，138.004542 秒，排程末尾 137.11 秒；11/11 机器回转写筛查通过，全文与视频同步人审批准，Audio Package 为 `human_reviewed` | 实体 App／现场验收 |
| Layer 4 | 三语页面信息人审后构建 15 个发布资产和 v2 catalog；Firebase Dev 修复版已部署。线上 169 文件 GET/SHA/Content-Type、三条 WAV Range 206、三语深链通过；浏览器三语文字与短时播放通过，旧三语片段页面保留。本机 iPhone Duo 模拟器已读取新片段三语页面并播放韩语音轨 | 实体 iPhone、现场验收分别核对 |
| Tracker | 本地进度账本和 Firebase Dev 公开实时页均为 46/46；三语 Layer 1–4 已记录。公开页显示 25 个步骤有实测执行、21 个已记录步骤缺实测执行 | 计数不代替审核、设备或现场验收；补齐缺失执行计时的原因分析 |

## 声纹与客户端

中、西、韩三语已用预计算的 15,371 个英文原声 landmarks 绑定各自已批准音轨的 SHA-256；韩语新索引 SHA-256 为 `d0a2f640435d48609bab76723e658ca5e7552bd469df8fdc7e3b37ff078afc1d`。以原视频 50 秒起的 10 秒声音在本机匹配，三份索引均返回 50.016 秒；韩语本次匹配 405 票、置信度 1。这些本机结果不能证明现场噪声中的真机麦克风效果。新版 App 增加独立页面的指纹绑定校验与同源下载；语言切换、来源或音轨变化会取消旧识别。三语索引已随 Dev 目录上架并逐文件核验。

新版 App 原先拒绝正式构建器输出的 `/media/<page>/<locale>.wav`；现已按页面、语言和哈希允许 WAV/MP3/M4A。本机实测又发现目录入口仍指向旧 `/multilingual.json`，且原生阅读器只接受含静态 HTML 的 `published_http_verified` 包；本次网页实际发布的是 `/multilingual-v2.json` 与保留 `candidate` 状态的哈希绑定内容 JSON。客户端已改为读取 v2；仅对 Firebase Dev 来源准入这类不可变候选包，校验内容哈希、语言和上游候选绑定后生成无脚本阅读页。正式来源继续要求已 HTTP 验证的静态页面包，不把 Dev 候选提升为正式包。

原生 `StorageTests` 15 项、Core 多语言目录定向测试实际执行 8 项、冻结 Dev 目录解码 1 项及新内容转义测试通过；真实 Firebase Dev 仓储测试依次读取新片段三语内容与完整 WAV，校验哈希通过。iPhone Duo iOS 27.1 的真实 Dev UI 测试选中新片段，三语各自标题在 WebView 可访问性树出现，韩语音轨完成下载并从 00:00 起播放；结果为 `artifacts/tongxing-ios/2026-09-24/cli/20260924T160152-test-51e5eee8/test.xcresult`。模拟器截图附件在本机导出后呈黑色，因此只据可访问性、下载/哈希与播放进度作结论。先前对齐控制器 20 项、独立页面 fixture UI 1 项及模拟器构建通过。App 阅读页和原生播放器目前是分开的交互；实体 iPhone、外放原声、耳机、锁屏、来电和现场麦克风识别尚未测试。

## 计时、并行和状态边界

本轮计时由 `artifacts/tracker-runs/2026-09-20-laodicea-clip/accounting/events.jsonl` 的执行事件生成，不按文件修改时间推算。当前公开 Tracker 显示 25 个步骤有实测执行、21 个已记录步骤缺实测执行；单次三语共享部署和线上核验只在中文发布步骤记录一次，韩／西的同一收据不虚构独立耗时。失败重试也保留。例：Layer 2 韩语翻译/逐组审查累计 372.895 秒、6 次执行中 4 次失败；这不是新修订单次耗时。各语言独立生产，三语正式音轨同时就绪后才汇合发布；逐步累计时间不代表全流程墙钟时间。

## Firebase Dev 发布与剩余验收

新页面已在 Firebase Dev 发布：[中文](https://ai-for-god-sermon-audio-dev.web.app/pages/2026-09-20-laodicea-clip/zh-Hans)、[韩语](https://ai-for-god-sermon-audio-dev.web.app/pages/2026-09-20-laodicea-clip/ko)、[西语](https://ai-for-god-sermon-audio-dev.web.app/pages/2026-09-20-laodicea-clip/es)。不可变 Release Package 保持构建时的 `candidate` 状态，独立 `http-verification-v3.json` 记录发布后通过；未事后改写包哈希。`browser-verification-v3.json` 记录三语各 11 组显示与短时播放（中文至 00:11、韩语至 00:14、西语至 00:19），以及旧韩语片段可打开、界面西语与内容韩语能独立切换。它们不代表整轨浏览器试听、iPhone 真机或现场验收。

发布前发现现有 Dev 站点的三个 HTML 已加入另一轮声音试听入口，旧 121 文件快照不再匹配。逐文件重读旧 121 项，仅这三项变化；另从线上目录取得并核验 33 个声音试听相关文件，组成 154 文件新基线。新页面首次部署后，HTTP 检查全通过，但浏览器目录读取器因额外声纹字段而拒绝新页面；修复两个正式目录读取模块、以当前已发布 169 文件快照做新基线预检后重新部署，最终浏览器通过。旧内容和声音试听目录在最终候选中保留。

韩语局部修订执行器只对变化的组重新调用 Astra 与 Sol，且只有请求哈希一致的旧组才复用缓存；定向 13 项测试通过。用户授权复用的 OpenAI Key 已保存在本项目被 Git 忽略、权限 `0600` 的 `.env` 中；`/v1/models` 实际请求返回 HTTP 200，列出 Astra 与 Sol。第 09–11 组已完成新模型请求，前 8 组按哈希复用，韩语插件与新候选准入通过；第 10 组一次空白差异导致校验失败，原始付费响应已保留并复用，修正后未重复付费。新候选经全文人审后，正式合成只复用声音指令完全一致的前 6 组；机器筛查与独立音频人审收据均绑定新轨，未沿用旧候选的人工批准。韩语本轮审核入口为 `review/audio-listening/ko-duration-revision.html`。

中文修正版的听审页是 `review/audio-listening/zh-Hans-corrected.html`，包含旧版入口、11 组新时间和完整音轨；新音轨 SHA-256 为 `2f011df221f91c35ae545267baabbacc7e746b134d9e05344c33705305b18b35`。旧版中文声纹索引绑定旧音轨哈希；已从相同的 15,371 个英文原声 landmarks 构造新音轨的绑定索引，SHA-256 为 `fc9a848099c9b7286e46b7b624ba97ed77eb0e2ef615f1cff5769b79a0eff6b1`。该索引已随 Dev 发布并通过线上文件哈希检查；真机麦克风与现场识别仍待验证。
