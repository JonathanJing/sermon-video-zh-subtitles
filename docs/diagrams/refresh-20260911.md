# 流程图更新记录 · 2026-09-11

11 张流程图已按当前项目契约校准并统一视觉样式，保留原文件名及文档引用。此次只更新图表及其维护资料。

## 生成方式

- 视觉稿：实际调用 `gpt-image-2.5-sunburst`，11 次请求成功，`1536x1024`、`high`；提示保存在 [imagegen-prompts.jsonl](imagegen-prompts.jsonl)。
- 最终产物：根据视觉稿重建原生 SVG，并依据项目文档修正模型生成稿的文字、日期与连接。文字可选择和编辑，无嵌入位图、脚本、外链资源。
- 后续维护：[diagram-specs.json](diagram-specs.json) 与 [render_diagrams.py](render_diagrams.py)。运行方法见 [索引](README.md)。
- 模型参考：[OpenAI 模型文档](https://developers.openai.com/api/docs/models/gpt-image-2.5-sunburst)。本次 CLI 未保存完整计费 usage，不据此声称精确成本。

## 内容校准

保留周六双 PDF、配音、Context Pack 与周日字幕的独立审批和验收要求；当前实时默认模型与 v4.1 本机实验明确分开。Agents API 图标明最小远端状态、本地受限工具、独立人工窗口审批与重新读取生产证据。历史云端图继续标明历史性质及旧 timeline Job 退役，不将历史路径当成当前生产。

图上的设计状态不代表现场验收。生成视觉稿中的内容不能代替最终 SVG 或来源文档。

## 验证与恢复

- 11 / 11 SVG 通过 XML 和 Chrome 实际渲染；666 个文字元素均在画布内，512 个带卡片边界标记的文字元素通过卡片容纳检查。
- 逐图检查中文、标题、条件分支和箭头；定向修复了双向连线标签及过宽的“只在本机”表述。连接几何检查未发现穿过无关卡片的线段。
- 生成器重复输出的文件哈希一致；保留 11 个原始 SVG 文件名，检查现有 Markdown 引用、源文件链接和 `git diff --check`。
- 这是文档与图表验证，没有重跑模型生产、后台完整测试或现场实验。

原图备份、11 张 Image 2.5 PNG、调用日志、渲染预览、检查结果及 SHA-256 清单保存在项目忽略目录 `artifacts/diagram-refresh-20260911/`。备份包含更新前已有修改的配音流程图。可在本机打开该目录中的 `gallery.html` 逐张查看最终图表；该目录不随 Git 分发。
